<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "hnote" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-white text-black font-mono">
    <nav class="border-b border-black px-6 py-4 flex items-center justify-between">
      <a href="<%= root_path %>" class="font-bold tracking-tighter text-xl">H.</a>
      <div class="flex items-center gap-6 text-sm">
        <% if user_signed_in? %>
          <%= link_to "New note", new_note_path, class: "border border-black px-3 py-1 hover:bg-black hover:text-white transition-colors" %>
          <span class="text-gray-500"><%= current_user.email %></span>
          <%= button_to "Sign out", destroy_user_session_path, method: :delete,
              class: "text-black hover:underline bg-transparent border-0 cursor-pointer p-0",
              data: { turbo_method: :delete } %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path %>
        <% end %>
      </div>
    </nav>

    <% if notice.present? %>
      <div class="border-b border-black px-6 py-2 text-sm"><%= notice %></div>
    <% end %>
    <% if alert.present? %>
      <div class="border-b border-black px-6 py-2 text-sm"><%= alert %></div>
    <% end %>

    <main class="px-6 py-8">
      <%= yield %>
    </main>
  </body>
</html>
